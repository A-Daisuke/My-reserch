const Benchmark = {
    run: function() {
        console.info(Math.random());
        console.log(Math.random());
        console.trace();
        console.warn(Math.random());
        console.error(Math.random());
    }
};