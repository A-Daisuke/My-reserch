const Benchmark = {
    run: function() {
        const FUNCTION_2 = (VAR_7) => VAR_7.split("").reverse().join("");
        console.log(`Hello world once again`);
        console.log(FUNCTION_2(`Hello world once again`));
    }
};